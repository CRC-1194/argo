values
{
    solver ( surfaceInitVolumeFraction );

    N_CELLS
    (
        16 32 64 128 
    );

    // Not used in this study, but must be set
    TRIANGLE_EDGE_LENGTH 
    (
        0.05
    );

    MAX_REFINEMENT_LEVEL
    (
        0 1 2 3 4 5 // Note: automatic mode not working properly for implicit surfaces
    );
}
