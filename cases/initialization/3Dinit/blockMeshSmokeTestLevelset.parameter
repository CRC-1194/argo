values
{
    solver ( surfaceCellVofInit );

    N_CELLS
    (
        16 32 64 
    );

    // Not used in this study, but must be set
    TRIANGLE_EDGE_LENGTH 
    (
        0.05
    );

    MAX_REFINEMENT_LEVEL
    (
        0 2 4 // Note: automatic mode not working properly for implicit surfaces
    );
}
